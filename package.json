{
  "name": "csv-cleaner-worker",
  "version": "0.1.0",
  "private": true,
  "description": "A Cloudflare Workers + TypeScript starter for a CSV cleaning portfolio tool.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('Test runner is not installed yet. See tests/ for planned cases.');\""
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
