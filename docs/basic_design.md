# CSV加工ツール 基本設計書

## 1. 概要

本ツールは、業務で頻繁に発生するCSVファイルの整形・変換作業をWeb上で簡単に実行できる軽量ツールである。

Excelや手作業で行われがちな以下の作業を、ブラウザ上で完結できるようにする。

- 不要列の削除
- 列名の変更
- 日付形式の統一
- 金額・数量などの数値整形
- 空白・全角半角・表記ゆれの補正
- 加工結果のプレビュー
- 加工済みCSVのダウンロード

主な目的は、実務で使われる「小さな業務改善ツール」としての完成度を示し、RPA・Excel・CSV自動化案件のポートフォリオとして活用することである。

---

## 2. 想定ユーザー

| ユーザー | 課題 |
|---|---|
| 事務担当者 | 毎月のCSV整形作業を手作業で行っている |
| EC運営担当者 | 売上データ・注文データの形式がシステムごとに異なる |
| 経理担当者 | 金額・日付・取引先名の表記を統一したい |
| 小規模事業者 | Excelマクロを使わずにCSVを加工したい |
| 副業・業務改善エンジニア | 自動化案件の提案時に見せられる実例が欲しい |

---

## 3. 解決したい課題

現場では、CSVデータをそのまま業務システムやExcel集計に使えないケースが多い。

例：

- 列名がシステムごとに異なる
- 日付形式が `2026/5/1`、`2026-05-01`、`20260501` など混在している
- 金額に `¥`、`,`、空白が含まれている
- 不要な列が多く、毎回削除している
- 名前や部署名に全角・半角スペースの揺れがある
- 欠損値や不正データを目視確認している

本ツールでは、これらの作業をルール化し、誰でも同じ加工結果を得られるようにする。

---

## 4. 主な機能

### 4.1 CSVアップロード

ユーザーはローカルPC上のCSVファイルをブラウザからアップロードできる。

#### 対応仕様

| 項目 | 内容 |
|---|---|
| ファイル形式 | `.csv` |
| 文字コード | UTF-8 / Shift_JIS |
| 区切り文字 | カンマ |
| ヘッダー行 | 1行目をヘッダーとして扱う |
| 最大ファイルサイズ | 初期版では 5MB 程度を想定 |

---

### 4.2 プレビュー表示

アップロード後、CSVの先頭数十行を表形式で表示する。

#### 表示内容

- 列名
- 行番号
- 各セルの値
- 欠損値や不正値の簡易ハイライト

---

### 4.3 加工ルール設定

ユーザーは画面上で加工ルールを選択できる。

#### 対応ルール

| ルール | 内容 | 例 |
|---|---|---|
| 不要列削除 | 指定した列を削除 | `メモ`, `内部管理ID` を削除 |
| 列名変更 | 列名を指定名に変更 | `顧客名` → `customer_name` |
| 日付形式統一 | 日付を `YYYY-MM-DD` に変換 | `2026/5/1` → `2026-05-01` |
| 数値整形 | カンマ・通貨記号を除去 | `¥12,000` → `12000` |
| 空白除去 | 前後の空白を除去 | ` 田中 ` → `田中` |
| 全角半角変換 | 数字・英字を半角へ変換 | `１２３` → `123` |
| 固定値追加 | 新しい列に固定値を追加 | `source=manual_upload` |
| 行フィルタ | 条件に合う行だけ残す | `status=paid` のみ残す |

---

### 4.4 加工結果プレビュー

加工前と加工後の差分を確認できる。

#### 表示方針

- 加工後の先頭50行を表示
- 列数・行数の変化を表示
- 削除された列・追加された列を表示
- エラー行がある場合は件数を表示

---

### 4.5 CSVダウンロード

加工後のCSVをダウンロードできる。

#### 出力仕様

| 項目 | 内容 |
|---|---|
| ファイル名 | `processed_YYYYMMDD_HHmmss.csv` |
| 文字コード | UTF-8 BOM付き / UTF-8 BOMなしを選択可能 |
| 改行コード | CRLF / LF を選択可能 |
| ヘッダー | あり |

---

### 4.6 加工ログ表示

実行した加工内容をログとして表示する。

#### ログ例

```text
[INFO] CSV file loaded: sample_sales_dirty.csv
[INFO] Rows: 12, Columns: 10
[INFO] Removed columns: memo, internal_id
[INFO] Renamed columns: 顧客名 -> customer_name, 購入日 -> order_date
[INFO] Normalized date column: order_date
[WARN] Invalid date detected at row 8: 2026/13/01
[INFO] Export completed: processed_20260528_143000.csv
```

---

## 5. 画面設計

### 5.1 画面一覧

| 画面 | 内容 |
|---|---|
| トップ画面 | CSVアップロード、機能説明 |
| プレビュー画面 | アップロードCSVの確認 |
| ルール設定画面 | 加工ルールの選択・設定 |
| 結果確認画面 | 加工後CSVのプレビュー |
| ダウンロード画面 | 加工済みCSVの取得、ログ確認 |

---

### 5.2 画面レイアウト案

```text
+--------------------------------------------------+
| CSV加工ツール                                    |
+--------------------------------------------------+
| 1. CSVアップロード                               |
| [ ファイルを選択 ] sample_sales_dirty.csv         |
|                                                  |
| 2. 加工ルール                                    |
| [x] 不要列を削除                                 |
| [x] 日付形式を統一                               |
| [x] 金額を数値化                                 |
| [ ] 固定値列を追加                               |
|                                                  |
| 3. プレビュー                                    |
| +----+------------+------------+----------+       |
| | No | 顧客名     | 購入日     | 金額     |       |
| +----+------------+------------+----------+       |
|                                                  |
| [ 加工を実行 ]  [ ダウンロード ]                 |
+--------------------------------------------------+
```

---

## 6. 処理フロー

```text
1. ユーザーがCSVファイルをアップロード
2. フロントエンドでファイル形式・サイズをチェック
3. CSVをパース
4. ヘッダー・行数・列数を取得
5. プレビューを表示
6. ユーザーが加工ルールを設定
7. 加工処理を実行
8. エラー・警告を検出
9. 加工後データをプレビュー表示
10. 加工済みCSVを生成
11. ユーザーがCSVをダウンロード
```

---

## 7. データ加工仕様

### 7.1 日付変換

#### 入力例

| 入力値 | 出力値 |
|---|---|
| `2026/5/1` | `2026-05-01` |
| `2026-05-01` | `2026-05-01` |
| `20260501` | `2026-05-01` |
| `2026年5月1日` | `2026-05-01` |
| `不明` | エラーまたは空欄 |

---

### 7.2 金額変換

#### 入力例

| 入力値 | 出力値 |
|---|---|
| `¥12,000` | `12000` |
| `12,000円` | `12000` |
| ` 3500 ` | `3500` |
| `無料` | `0` またはエラー |
| `-1,200` | `-1200` |

---

### 7.3 空白除去

#### 対象

- セル前後の半角スペース
- セル前後の全角スペース
- 連続スペースの正規化

#### 例

| 入力値 | 出力値 |
|---|---|
| ` 田中 太郎 ` | `田中 太郎` |
| `　営業部　` | `営業部` |
| `山田  花子` | `山田 花子` |

---

### 7.4 列名変換

#### 例

| 変換前 | 変換後 |
|---|---|
| `注文ID` | `order_id` |
| `顧客名` | `customer_name` |
| `購入日` | `order_date` |
| `商品名` | `product_name` |
| `金額` | `amount` |
| `ステータス` | `status` |

---

## 8. エラーハンドリング

| エラー | 原因 | 対応 |
|---|---|---|
| ファイル形式エラー | CSV以外をアップロード | エラーメッセージを表示 |
| 文字コードエラー | 読み取り不能な文字コード | UTF-8 / Shift_JIS を切替 |
| ヘッダー不正 | ヘッダー行がない | 処理を停止 |
| 日付変換エラー | 不正な日付 | 該当行を警告表示 |
| 数値変換エラー | 金額欄に文字列 | 空欄またはエラーとして扱う |
| 空ファイル | データ行がない | 処理を停止 |
| サイズ超過 | 最大サイズ超過 | アップロード前に拒否 |

---

## 9. セキュリティ・プライバシー

初期版では、アップロードされたCSVはサーバーに保存しない設計とする。

### 方針

- ファイルはブラウザまたは一時メモリ上で処理する
- 加工後、サーバー側にCSVを永続保存しない
- 個人情報を扱う可能性があるため、ログにはセル内容を過剰に出力しない
- 公開デモではサンプルCSVのみ使用する
- 本番利用を想定する場合は、認証・通信暗号化・アクセス制御を検討する

---

## 10. 技術構成案

### 10.1 フロントエンド

| 項目 | 候補 |
|---|---|
| 言語 | TypeScript |
| UI | React / Vite |
| CSVパース | Papa Parse |
| スタイリング | Tailwind CSS |
| デプロイ | Cloudflare Pages |

---

### 10.2 バックエンド

初期版ではサーバーレス構成を想定する。

| 項目 | 候補 |
|---|---|
| 実行環境 | Cloudflare Workers |
| 言語 | TypeScript |
| ストレージ | 原則なし |
| ログ | Cloudflare Logs / console log |
| API | `/api/convert` |

---

### 10.3 処理方式

| 方式 | 内容 | 初期版での採用 |
|---|---|---|
| ブラウザ内処理 | CSVをブラウザ上で加工 | 採用候補 |
| Worker API処理 | CSVをWorkerに送信して加工 | 採用候補 |
| D1保存 | 加工履歴をDB保存 | 後続拡張 |
| R2保存 | CSVファイル保存 | 後続拡張 |

初期版では、個人情報保護と実装の軽さを重視し、ブラウザ内処理を基本とする。複雑な加工やテンプレート保存機能を追加する場合は、Worker APIやD1を利用する。

---

## 11. API設計案

### 11.1 CSV変換API

#### エンドポイント

```http
POST /api/convert
```

#### リクエスト

```json
{
  "csvText": "注文ID,顧客名,購入日,金額\n1001,田中太郎,2026/5/1,¥12,000",
  "options": {
    "removeColumns": ["メモ", "内部管理ID"],
    "renameColumns": {
      "注文ID": "order_id",
      "顧客名": "customer_name",
      "購入日": "order_date",
      "金額": "amount"
    },
    "normalizeDateColumns": ["order_date"],
    "normalizeNumberColumns": ["amount"],
    "trimWhitespace": true
  }
}
```

#### レスポンス

```json
{
  "success": true,
  "summary": {
    "inputRows": 12,
    "outputRows": 11,
    "inputColumns": 10,
    "outputColumns": 6,
    "warnings": 2
  },
  "csvText": "order_id,customer_name,order_date,amount\n1001,田中太郎,2026-05-01,12000",
  "warnings": [
    {
      "row": 8,
      "column": "order_date",
      "value": "2026/13/01",
      "message": "Invalid date format"
    }
  ]
}
```

---

## 12. テスト方針

### 12.1 正常系テスト

| No | 観点 | 入力 | 期待結果 |
|---|---|---|---|
| 1 | CSV読込 | 正常なCSV | プレビュー表示される |
| 2 | 列削除 | `メモ`列を削除 | 出力CSVに含まれない |
| 3 | 列名変更 | `顧客名` → `customer_name` | ヘッダーが変更される |
| 4 | 日付変換 | `2026/5/1` | `2026-05-01` になる |
| 5 | 金額変換 | `¥12,000` | `12000` になる |
| 6 | 空白除去 | ` 田中 ` | `田中` になる |
| 7 | CSV出力 | 加工後データ | ダウンロードできる |

---

### 12.2 異常系テスト

| No | 観点 | 入力 | 期待結果 |
|---|---|---|---|
| 1 | 空ファイル | 0バイトCSV | エラー表示 |
| 2 | ヘッダーなし | データ行のみ | エラー表示 |
| 3 | 不正日付 | `2026/13/01` | 警告表示 |
| 4 | 不正金額 | `金額未定` | 警告表示 |
| 5 | 列不足 | 必須列なし | 警告表示 |
| 6 | CSV以外 | `.xlsx` | アップロード不可 |
| 7 | サイズ超過 | 5MB超過 | アップロード不可 |

---

## 13. サンプルCSV

リポジトリには以下のサンプルCSVを配置する。

| ファイル名 | 用途 |
|---|---|
| `sample_sales_dirty.csv` | 表記ゆれ・空白・不正値を含む売上CSV |
| `sample_sales_expected.csv` | 加工後の期待結果CSV |
| `sample_attendance_dirty.csv` | 勤怠データの表記ゆれ確認用 |
| `sample_attendance_expected.csv` | 勤怠データの期待結果 |

---

## 14. READMEに記載するアピールポイント

- 実務でよくあるCSV整形作業を想定
- 加工前後の差分が確認できる
- 不正データを警告として可視化
- サーバーにCSVを保存しない設計
- TypeScriptで加工ロジックを分離
- テストCSVと期待結果CSVを用意
- RPA・Excel業務改善案件への応用を想定

---

## 15. 今後の拡張案

| 拡張 | 内容 |
|---|---|
| 加工テンプレート保存 | よく使う加工ルールを保存 |
| 複数CSV結合 | 同じ形式のCSVをまとめて結合 |
| Excel出力 | `.xlsx` 形式で出力 |
| Slack通知 | 加工完了をSlackに通知 |
| D1連携 | 加工履歴を保存 |
| API化 | 外部システムからCSV変換APIを呼び出す |
| バッチ処理 | 定期的なCSV加工を自動実行 |

---

## 16. 成果物イメージ

```text
csv-cleaner-tool/
├── README.md
├── docs/
│   └── basic_design.md
├── samples/
│   ├── sample_sales_dirty.csv
│   ├── sample_sales_expected.csv
│   ├── sample_attendance_dirty.csv
│   └── sample_attendance_expected.csv
├── src/
│   ├── components/
│   ├── lib/
│   │   ├── csvParser.ts
│   │   ├── transformers.ts
│   │   └── validators.ts
│   └── pages/
└── tests/
    ├── transformers.test.ts
    └── validators.test.ts
```

---

## 17. このツールで示せるスキル

- CSVデータ加工の実務理解
- TypeScriptによるデータ変換ロジック実装
- エラーハンドリング設計
- UI/UXを意識したプレビュー機能
- セキュリティ・個人情報への配慮
- テストデータ・期待結果を用意した品質管理
- 業務改善・RPA案件への応用力
